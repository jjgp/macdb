syntax = "proto3";

package MacDB;

service Window {
    rpc Capture (WindowInfo) returns (stream WindowCapture) {}
}

message WindowInfo {
    string name = 1;
}

message WindowCapture {
    string image = 1;
}